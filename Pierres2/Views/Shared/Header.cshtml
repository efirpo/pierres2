<div id="header">
  <img src="~/img/title.png"><br>
  | @Html.ActionLink("Treats Index", "Index", "Treats") | 
  @Html.ActionLink("Flavors Index", "Index", "Flavors") | @if (User.Identity.IsAuthenticated)
  {
    @using (Html.BeginForm("LogOff", "Account"))
    {
      <input type="submit" class="btn btn-default" value="Log out" />
    }
  }
  else
  {
   
      <input type="button" class="btn btn-default" value="Log in" onclick="window.location.href = 'Account/Login'"/>

  }
</div>